---
import svgNew from '../assets/new.svg'

const base = import.meta.env.BASE_URL

const testimonials = [
  {
    comment: `"He showcases ability to bridge theoretical knowledge with practical applications to real-world problems."`,
    author: {
      name: `Dr. Gopal Nanda`,
      pos: `Professor of Engineering Mathematics, SAKEC`,
      pic: `${base}review/profile_photo_gopal.jpg`
    },
    cls: `rotate-2 -translate-y-10`
  },
  {
    comment: `"Played a pivotal role in the seamless functionality and appealing aesthetics of the project."`,
    author: {
      name: `Milind Khairnar`,
      pos: `Professor of AI & Data Science, SAKEC`,
      pic: `${base}review/profile_photo_milind.webp`
    },
    cls: `-rotate-2 -translate-x-10  z-5`
  },
  {
    comment: `"He was ready with a roadmap and weekly objectives... even when faced with challenges, he was able to problem-solve and find an ultimately more effective method."`,
    author: {
      name: `Leila Boushehri`,
      pos: `Data Scientist, Sense Worldwide`,
      pic: `${base}review/profile_photo_leila.jpg`
    },
    cls: `rotate-2 translate-x-5 z-5`
  },
]
---


<section>
    <div class="max-w-4xl mx-auto pb-20 px-10 overflow-hidden">
      <div class="grid grid-cols-1 md:grid-cols-2">
        <div class="flex relative">
          <h1 class="text-3xl">Here's what people are saying about my work</h1>
          <img src={svgNew.src} alt="arrow" width="40" class="absolute -right-3 md:-right-10 bottom-0" />
        </div>
      </div>
  
  
      <div class="grid grid-cols-1 md:grid-cols-2 py-30">
        {testimonials.map(item => ( 
          <div class={`flex flex-col gap-3 p-5 pr-15 bg-white border-2 shadow-[6px_6px_0px_rgba(0,0,0,0.3)] ${item.cls}`}>
            <p>{item.comment}</p>
            <div class="flex items-center gap-2">
              <div><div class="size-11 rounded-full border overflow-hidden">
                  <img src={item.author.pic} alt="" class="w-full h-full object-cover grayscale" />
              </div></div>
              <div>
                <h3 class="font-medium">{item.author.name}</h3>
                <p>{item.author.pos}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
  
    </div>
   </section>
